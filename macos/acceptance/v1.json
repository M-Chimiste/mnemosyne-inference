{
  "schema_version": 1,
  "product": "Unified Inference for macOS",
  "last_updated": "2026-08-30",
  "candidate_version": "0.9.0",
  "release_version": "1.0.0",
  "release_ready": false,
  "latest_local_artifact": {
    "filename": "Unified-Inference-0.9.0-macos-arm64.dmg",
    "sha256": "c830fafaf08650eef055a7bdcff296481ff900ce2b329f6874c171ff884837ad",
    "bytes": 309978811,
    "signature": "ad-hoc",
    "notarized": false,
    "launchable": true,
    "acceptance_report": "Unified-Inference-0.9.0-macos-arm64-acceptance.json",
    "verification": "Build 50 from implementation commit 8fd50e9 supersedes rejected build 47. It was mounted read-only with the Applications shortcut; deep signature, embedded import/configuration, zero-bytecode, Sparkle dependency, packaged framework, and Contents/Frameworks LC_RPATH checks all pass in the mode-0600 secret-redacted acceptance report."
  },
  "tiers": {
    "stable": ["core", "llama.cpp", "omlx"],
    "preview": ["ds4", "mflux"]
  },
  "gates": [
    {
      "id": "automated-native-suites",
      "required": true,
      "status": "passed",
      "evidence": "Local Apple Silicon run: service 280 passed (278 in the restricted suite plus both real-bookmark tests with host access); image worker 23 passed with Metal access; Swift 60 passed; packaging 33 passed; version and locked-runtime checks passed. Runtime-link verification now rejects the launch-dead build-47 app that the earlier signature-only checks accepted."
    },
    {
      "id": "github-macos-ci",
      "required": true,
      "status": "passed",
      "evidence": "GitHub Actions macOS CI run 30265133377 passed every version/lock, native service, image worker, packaging, Swift build/test, runtime-link-aware bare-app verification, acceptance-report, and artifact-upload step for implementation commit 8fd50e9. The signed release workflow remains gated separately on Apple and Sparkle credentials."
    },
    {
      "id": "mac-pool-pairing-and-self-revoke",
      "required": true,
      "status": "pending",
      "evidence": "The default-off Hub and Mac pairing foundations, distinct role credentials, pinned activation, exact-request local revocation fence, credential tombstone, malformed/oversized/redirect ambiguity recovery, fail-closed corrupt tombstones, and bounded retired invitation/pairing/credential-bundle fingerprint fences have automated coverage. Tests reject restoration of the old pairing identity or exact old bearer bundle without persisting bearer values. Routine credential rotation, remote administrator-to-Mac notification, and static-enrollment adoption remain implementation gaps. A signed two-Mac ceremony, Nyx/Mac restart, offline revocation/re-pair, administrator revoke/forget behavior, and proof that the actually issued retired bearers receive no Hub or Mac authority still require production multi-host artifact evidence."
    },
    {
      "id": "mac-pool-participation-limited-overflow",
      "required": true,
      "status": "pending",
      "evidence": "Automated coverage exercises durable local join/pause, full-response participation leases, closed paused capacity, local-request independence, Hub service classes, and limited-worker overflow ordering. Signed multi-host evidence must still prove drain during a live stream, restart while paused, cold JIT rejoin, and Nyx-local worker process/resource isolation under saturation without degrading the Hub."
    },
    {
      "id": "mac-pool-inventory-placement-download",
      "required": true,
      "status": "pending",
      "evidence": "Strict path-free inventory and desired-install schemas, authenticated synchronization, signed-recipe placement scoring, explicit Mac/storage selection, durable Mac-local execution, and exact storage-generation fencing have automated coverage. Exact lexical weight-location regressions exercise a nested symlink-selected root through resolution, download, cold registration, JIT inference/accounting, and in-place migration with no default-folder fallback. An active Settings rebind remains denied across restart, while a pre-claim legacy downloaded row fails closed instead of adopting current volume/scope authority; both retain the original bytes and avoid redownload/profile registration. Representative signed Macs must still prove complete inventory reconciliation and redaction, recommendations across hardware tiers, selection of a non-default Mac and exact internal/external registered folder, restart/resume and response-loss convergence, and no fallback relocation or Hub-proxied weight bytes."
    },
    {
      "id": "mac-pool-jit-batching-accounting",
      "required": true,
      "status": "pending",
      "evidence": "Existing automated suites retain cold-model JIT loading, one global resident target, warm-first routing, bounded queues, full-stream leases, engine-owned batching/capacity, idle unloading, and content-free durable usage events. The exact signed candidate still needs multi-Mac concurrent streaming and non-streaming evidence proving one load per cold target, correct serving-device attribution, exactly one ledger row per completed route through Nyx/ledger outages, and matching bounded pool aggregates."
    },
    {
      "id": "signed-compatibility-catalog",
      "required": true,
      "status": "pending",
      "evidence": "The closed Apple Silicon catalog schema, signature verifier, monotonic last-known-good activation, failure isolation, update client, cross-component contract fixtures, and offline encrypted-key/detached-signature/multi-signer assembly/exact-byte publication ceremony have automated coverage. The ceremony rejects the repository test key and fences publication against prior sequence/digest state. No production trust anchor, canonical HTTPS publication endpoint, or production-signed expiry-valid catalog with exact hardware/runtime recipes has been accepted on the release artifact; DeepSeek V4 Flash and GLM 5.3 Flash remain unclaimed until their exact recipes pass representative compatibility evidence."
    },
    {
      "id": "model-first-runtime-preparation",
      "required": true,
      "status": "pending",
      "evidence": "Catalog-bound model and runtime contracts are implemented without granting the Hub filesystem or engine authority. Claim-backed and current-attempt native installs retain the exact user-selected lexical storage binding; registration refuses a changed root, destination, volume, scope, or revision, and legacy post-download rows without durable/current-attempt authority fail closed rather than infer it from current Settings. The signed app has not yet demonstrated the complete Stable model-first flow on a clean supported Mac: choose a model, obtain or validate its required runtime without a terminal or source checkout, select an exact registered folder, verify and register weights, leave the model cold, then JIT-load and infer successfully."
    },
    {
      "id": "executable-migration-rollback-uninstall",
      "required": true,
      "status": "pending",
      "evidence": "Path-free previews, exact private retention manifests, durable transaction claims, rollback fencing, and the primitive-free lifecycle orchestration core have automated coverage. Production effects deliberately remain disabled: no accepted signed helper/runner authority executes the closed effect graph. Signed existing-Mac migration, forced rollback, all uninstall retention levels, offline Hub-revocation recovery, and reinstall discovery with exact weights/storage preserved remain required."
    },
    {
      "id": "guided-clean-install",
      "required": true,
      "status": "pending",
      "evidence": "The candidate app now records first presentation separately from completion with exact version/build and ordered timestamps, and the strict collector combines those markers with its durable-usage self-test. A stale completion preference cannot pass. The real clean signed installation, preference reset, and visible first-run UI pass remain pending."
    },
    {
      "id": "login-and-launchagent-lifecycle",
      "required": true,
      "status": "pending",
      "evidence": "The acceptance collector proves exact-label restart/KeepAlive recovery and can now compare a private accepted pre-cycle report with the same host/build after logout/login or reboot. The latter requires a changed launchctl GUI audit-session ID and PID, healthy listeners, and fresh durable inference, so an ordinary restart cannot pass. The current candidate still requires the real target-Mac cycle."
    },
    {
      "id": "lmstudio-finder-migration",
      "required": true,
      "status": "pending",
      "evidence": "The strict live collector now verifies the post-adoption native alias, consumed inert migration row, hinted-library containment, offline LM Studio listener, and successful request. The current candidate still needs the real Finder adoption and historical alias/load-setting comparison on the target Mac."
    },
    {
      "id": "llamacpp-real-text-and-vision",
      "required": true,
      "status": "pending",
      "evidence": "Text was previously exercised; candidate vision and durable usage evidence remain required."
    },
    {
      "id": "omlx-real-request-and-usage",
      "required": true,
      "status": "pending",
      "evidence": "Gemma request and token accounting were previously observed; candidate build rerun remains required."
    },
    {
      "id": "omlx-durable-startup-auth-recovery",
      "required": true,
      "status": "pending",
      "evidence": "The strict live collector can now combine exact core restart/KeepAlive, authoritative reconciliation, oMLX readiness/auth inspection, and a durable oMLX self-test. The external service still must survive a real login/startup with its configured model directories on the current candidate."
    },
    {
      "id": "postgres-idempotent-delivery",
      "required": true,
      "status": "pending",
      "evidence": "Candidate outbox write and remote drain must be captured."
    },
    {
      "id": "protected-folder-restart",
      "required": true,
      "status": "pending",
      "evidence": "The strict live collector now requires a receiver scope ID, persisted volume identity, healthy matching storage, exact service restart/KeepAlive, and a successful post-restart manager-owned llama.cpp request. A real Finder selection and candidate run are still required."
    },
    {
      "id": "durable-download-lifecycle",
      "required": true,
      "status": "pending",
      "evidence": "Native installs now retain a compact, non-secret transition journal even after history is hidden or files are deleted, and the strict collector rejects migrated snapshots as invented history. Real candidate download, cancel/retry completion, downloaded-weight registration retry, history removal, exact revision pin, and managed deletion actions are still required."
    },
    {
      "id": "managed-runtime-update-rollback",
      "required": true,
      "status": "pending",
      "evidence": "Earlier llama.cpp update/rollback hardware smoke passed. The current candidate now retains a bounded mode-0600 lifecycle journal with anonymous service-instance IDs and fixed failure codes; the strict collector requires activation, restarted inference on the update, rollback, restarted inference on the baseline, integrity/path-safety rejection, and a fresh installed snapshot proving the baseline remained active. The real target-Mac multi-pass exercise is still pending."
    },
    {
      "id": "developer-id-notarized-dmg",
      "required": true,
      "status": "pending",
      "evidence": "Build 47 was rejected after a real installation exposed a missing Contents/Frameworks LC_RPATH for Sparkle. The corrected 309,978,811-byte 0.9.0 build-50 arm64 ad-hoc DMG from implementation commit 8fd50e9 (SHA-256 c830fafaf08650eef055a7bdcff296481ff900ce2b329f6874c171ff884837ad) passes both independent runtime-link checks and its mounted mode-0600 acceptance report. It is not a distribution artifact; Apple signing/notarization and Sparkle private-key credentials remain unavailable in this checkout."
    },
    {
      "id": "signed-update-and-recovery",
      "required": true,
      "status": "pending",
      "evidence": "Requires two Developer ID notarized builds and the production signed appcast."
    },
    {
      "id": "ds4-preview-isolation",
      "required": true,
      "status": "passed",
      "evidence": "Fresh Python, packaged YAML, and Swift defaults leave DS4 disabled; readiness labels it Preview; adapter/coordinator tests prove authoritative stopped absence and fail-closed unknown-process handling. Real DS4 inference remains a post-V1 Preview promotion gate."
    },
    {
      "id": "mflux-preview-isolation",
      "required": true,
      "status": "passed",
      "evidence": "Fresh Python, packaged YAML, and Swift defaults leave MFLUX disabled; readiness labels it Preview; worker and adapter tests cover isolated lifecycle and unmanaged-listener failure. Real cancellation/Metal-release coverage remains a post-V1 Preview promotion gate."
    }
  ]
}
